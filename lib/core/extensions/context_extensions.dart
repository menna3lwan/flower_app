/// Relocated: `BuildContext` extensions are UI-tree-specific and now
/// live under `lib/common/extensions`, keeping `core/extensions` limited
/// to framework-agnostic (pure Dart) helpers. See
/// `lib/common/extensions/context_extensions.dart`.
export '../../common/extensions/context_extensions.dart';
