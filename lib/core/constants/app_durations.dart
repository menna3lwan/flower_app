/// Named timing constants — the duration-token equivalent of [AppDimens].
abstract final class AppDurations {
  const AppDurations._();

  static const Duration splashDisplay = Duration(milliseconds: 1200);
  static const Duration snackBarDefault = Duration(seconds: 4);
}
