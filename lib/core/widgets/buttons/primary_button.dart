/// Relocated: reusable UI widgets now live under `lib/common/widgets`,
/// keeping `core` strictly to infrastructure concerns. This file is kept
/// as a re-export so any stale import path still resolves.
export '../../../common/widgets/buttons/primary_button.dart';
