# assets/icons

Custom SVG/PNG icon assets that aren't already covered by Material
Icons (`Icons.xxx`) or Cupertino Icons.

Every icon currently used in the app is a Material icon referenced
directly in code (no asset needed). Add a file here only when the
design calls for a bespoke icon Material doesn't provide, and register
its path in `lib/core/constants/app_assets.dart`.
